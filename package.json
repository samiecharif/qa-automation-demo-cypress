{
  "name": "qa-automation-demo-cypress",
  "version": "1.0.0",
  "private": true,
  "description": "Demo d'automatisation de tests avec Cypress + Allure",
  "scripts": {
    "cy:open": "cypress open",
    "test": "cypress run",
    "report:allure": "allure generate reports/allure-results --clean -o reports/allure-report && allure open reports/allure-report"
  },
  "devDependencies": {
    "cypress": "^13.7.0",
    "@shelex/cypress-allure-plugin": "^2.40.2",
    "allure-commandline": "^2.29.0"
  }
}